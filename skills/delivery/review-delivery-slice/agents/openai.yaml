interface:
  display_name: "Review Delivery Slice"
  short_description: "Review one snapshot and persist findings"
  default_prompt: "Use $review-delivery-slice to review the assigned fixed snapshot on independent Standards and Spec axes and persist exact-snapshot receipts."
policy:
  allow_implicit_invocation: false
