interface:
  display_name: "Author Slice Tests"
  short_description: "Author and validate one slice's tests"
  default_prompt: "Use $author-slice-tests in red-contract, green-baseline, green-finalization, or rebase-conflict mode for this delivery slice."
policy:
  allow_implicit_invocation: false
