# AGENTS

- Do not introduce compiler warnings. Fix any warnings before committing or opening a PR.
- Always build and run tests locally before pushing, to validate your work.
- Always use `///` XML doc comments wherever supported (public/internal/private methods, properties, types, const fields). Use `//` only for inline implementation notes where `///` is not supported. Never for internal/private fields.
